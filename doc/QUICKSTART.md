# 快速开始

## 启动游戏

```bash
cd PetPlan
python3 -m http.server 4174 --directory .
```

在浏览器访问 [http://localhost:4174](http://localhost:4174)。项目依赖 ES Modules，不支持直接使用 `file://` 打开。

PetPlan 的游戏舞台固定为横屏 16:9。手机或平板竖屏时会显示旋转设备提示。

## 第一轮玩法

1. 在命运场景点击桌面硬币，获得正面或反面资源。
2. 查看右侧“推荐”商店，优先购买当前可负担的成长项。
3. 购买桌面硬币、小助手或自动结算后，观察命运循环加速。
4. 进入领地并从西侧入口开始宠物远征，使用 WASD、方向键或屏幕方向键探索大地图和小地图迷雾。
5. 在终端选择追踪目标，亲自走到附近后按 E 或点击“交互”；搜索地点只需选择一次拿取方式，遇敌后使用鼠标或触摸战场瞄准射击。
6. 清理至少 3 个地点后，根据生命、威胁和背包价值决定继续深入，或返回地图西侧入口启动撤离。
7. 留在撤离圈内守住倒计时，将金币、水晶和经验带回局外，再去领地建造建筑形成长期成长。

## 宠物远征速览

- 每局从西侧入口出发，拥有满生命、2 份补给和 8 格空背包；角色可在整张地图上自由移动。
- 小地图只显示已经揭开的迷雾区域。点击地点只会追踪方向和距离，不会自动寻路；靠近后按 E 或点击“交互”。
- 快速拿取耗时短、警戒低；彻底搜刮获得更多物资但会明显提高警戒。宠物探索天赋会自动强化对应方式。
- 出发前选择一把武器，进入远征后保持锁定。鼠标或触摸战场用于瞄准射击，空弹匣会自动换弹。
- 只有队长宠物提供一个主动队伍技能，其余上阵宠物继续提供攻击、护卫和探索被动。
- 补给可以恢复生命；安全屋还能回血、降低威胁并补充补给。
- 清理 3 个地点后需要返回入口信标撤离。离开撤离圈只会暂停倒计时，战斗仍会继续。
- 撤离成功才能带回全部背包物品。战败或放弃会遗失背包，只保留部分战斗收益。

## 快捷键

| 快捷键 | 行为 |
| --- | --- |
| WASD / 方向键 | 在远征大地图中移动 |
| E | 靠近远征地点后交互 |
| 鼠标 / 触摸战场 | 瞄准并射击 |
| Q | 释放队伍技能 |
| Tab / M | 展开或收起远征终端 |
| F5 | 保存到槽位 1 |
| F9 | 从槽位 1 加载 |
| Escape | 远征中打开退出确认；其他场景关闭当前弹窗 |

## 常见问题

**页面提示需要本地服务**：在项目根目录运行 `python3 -m http.server 4174 --directory .`，再打开 `http://localhost:4174`。

**竖屏只显示旋转提示**：这是预期行为。请将设备转为横屏。

**旧的 `territory.html` 链接**：该页面会自动跳转到主游戏内的领地场景，存档和资源与主游戏一致。
